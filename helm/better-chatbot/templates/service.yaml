apiVersion: v1
kind: Service
metadata:
  name: {{ include "better-chatbot.fullname" . }}
  labels:
    {{- include "better-chatbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "better-chatbot.selectorLabels" . | nindent 4 }}